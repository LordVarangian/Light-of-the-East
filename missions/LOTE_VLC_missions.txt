lote_vlc_one = {
	slot = 1
	generic = no 
	ai = yes
	potential = {
		tag = VLC
	}
	has_country_shield = yes
	
	vlc_increase_development = {
		icon = mission_eastern_european_city
		trigger = {
			total_development = 50
		}
		effect = {
			wallachia_area = {
				if = {
					limit = {
						NOT = {
							owned_by = ROOT
							is_core = ROOT
							type = all
						}
					}
					add_permanent_claim = ROOT
				}
			}
			159 = {
				if = {
					limit = {
						NOT = {
							owned_by = ROOT
							is_core = ROOT
						}
					}
					add_permanent_claim = ROOT
				}
			}
			add_country_modifier = {
				name = lote_vlc_preparations_for_war
				duration = 10950
			}
		}
	}
}
			